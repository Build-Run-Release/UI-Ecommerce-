<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promote Item | Campus Market</title>
    <link rel="stylesheet" href="/css/campus-theme.css">
    <style>
        .pricing-option {
            border: 2px solid var(--border);
            padding: 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.2s;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .pricing-option:hover,
        .pricing-option.selected {
            border-color: var(--primary);
            background: #eff6ff;
        }

        .pricing-option input {
            transform: scale(1.2);
            accent-color: var(--primary);
        }
    </style>
</head>

<body
    style="display:flex; align-items:center; justify-content:center; min-height:100vh; background: #f8fafc; padding: 2rem;">

    <div class="card" style="width: 100%; max-width: 500px; padding: 0; overflow: hidden;">

        <div style="background: #1e293b; color: white; padding: 2rem; text-align: center;">
            <h1 style="font-size: 1.5rem; font-weight: bold; margin-bottom: 0.5rem;" class="text-white">Boost Your Sales
                🚀</h1>
            <p style="opacity: 0.8; font-size: 0.9rem;">Pin your items to the Campus Homepage.</p>
        </div>

        <div style="padding: 2rem;">
            <form action="/ads/paystack/initialize" method="POST" class="flex flex-col gap-4">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

                <div>
                    <label class="text-xs font-bold text-muted uppercase">Short Ad Message</label>
                    <textarea name="message" class="input-base" rows="3"
                        placeholder="e.g. 50% OFF on all Textbooks! Check out my profile." required
                        maxlength="100"></textarea>
                </div>

                <div>
                    <label class="text-xs font-bold text-muted uppercase">Select Duration</label>
                    <div class="flex flex-col gap-3" style="margin-top: 0.5rem;">

                        <label class="pricing-option selected" onclick="selectOption(this)">
                            <input type="radio" name="plan" value="1_day_500" checked>
                            <div class="flex-1">
                                <div class="font-bold">24 Hours Boost</div>
                                <small class="text-muted">Great for flash sales</small>
                            </div>
                            <div class="font-bold text-primary">₦500</div>
                        </label>

                        <label class="pricing-option" onclick="selectOption(this)">
                            <input type="radio" name="plan" value="3_day_1200">
                            <div class="flex-1">
                                <div class="font-bold">3 Days Boost</div>
                                <small class="text-muted">Best value for weekends</small>
                            </div>
                            <div class="font-bold text-primary">₦1,200</div>
                        </label>

                        <label class="pricing-option" onclick="selectOption(this)">
                            <input type="radio" name="plan" value="7_day_2500">
                            <div class="flex-1">
                                <div class="font-bold">7 Days Boost</div>
                                <small class="text-muted">Maximum visibility</small>
                            </div>
                            <div class="font-bold text-primary">₦2,500</div>
                        </label>

                    </div>
                </div>

                <div style="margin-top: 1rem;">
                    <button class="btn btn-primary" style="width: 100%;">Pay & Publish Ad</button>
                    <a href="/seller/dashboard" class="btn btn-secondary"
                        style="width: 100%; margin-top: 0.5rem; text-align: center;">Cancel</a>
                </div>

            </form>
        </div>

    </div>

    <script>
        function selectOption(el) {
            document.querySelectorAll('.pricing-option').forEach(opt => opt.classList.remove('selected'));
            el.classList.add('selected');
        }
    </script>

</body>

</html>