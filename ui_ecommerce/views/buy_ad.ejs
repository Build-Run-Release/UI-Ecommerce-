<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promote Your Items - UI Ecommerce</title>
    <style>
        /* 1. Base Styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* 2. Container */
        .ad-container {
            background: white;
            width: 100%;
            max-width: 500px;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #667eea, #764ba2);
            padding: 30px;
            color: white;
            text-align: center;
        }

        .header h1 { margin: 0; font-size: 1.5rem; }
        .header p { margin: 10px 0 0; opacity: 0.9; }

        .body { padding: 30px; }

        /* 3. Form Elements */
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #444;
        }

        textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
            font-family: inherit;
            resize: vertical;
            margin-bottom: 20px;
        }

        /* 4. Pricing Options (Radio Cards) */
        .pricing-options {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 25px;
        }

        .price-option {
            display: flex;
            align-items: center;
            border: 1px solid #eee;
            padding: 15px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
        }

        .price-option:hover {
            border-color: #764ba2;
            background-color: #f8f9fa;
        }

        .price-option input { margin-right: 15px; transform: scale(1.2); }
        
        .option-details { flex: 1; }
        .option-title { font-weight: bold; color: #333; }
        .option-price { color: #28a745; font-weight: bold; }

        /* 5. Pay Button */
        .btn-pay {
            background-color: #28a745;
            color: white;
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
        }
        .btn-pay:hover { background-color: #218838; }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #666;
            text-decoration: none;
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="header">
            <h1>Boost Your Sales</h1>
            <p>Get your message pinned to the Homepage.</p>
        </div>

        <div class="body">
            <form action="/ads/paystack/initialize" method="POST">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                
                <label>Ad Message</label>
                <textarea name="message" rows="3" placeholder="e.g. 50% OFF on all Laptops this weekend! Visit my profile." required maxlength="100"></textarea>

                <label>Select Duration</label>
                <div class="pricing-options">
                    
                    <label class="price-option">
                        <input type="radio" name="plan" value="1_day_500" checked>
                        <div class="option-details">
                            <div class="option-title">24 Hours Boost</div>
                            <small style="color: #666;">Great for flash sales</small>
                        </div>
                        <div class="option-price">₦500</div>
                    </label>

                    <label class="price-option">
                        <input type="radio" name="plan" value="3_day_1200">
                        <div class="option-details">
                            <div class="option-title">3 Days Boost</div>
                            <small style="color: #666;">Best value for weekend</small>
                        </div>
                        <div class="option-price">₦1,200</div>
                    </label>

                    <label class="price-option">
                        <input type="radio" name="plan" value="7_day_2500">
                        <div class="option-details">
                            <div class="option-title">7 Days Boost</div>
                            <small style="color: #666;">Maximum visibility</small>
                        </div>
                        <div class="option-price">₦2,500</div>
                    </label>

                </div>

                <button type="submit" class="btn-pay">Pay & Publish Ad</button>
            </form>

            <a href="/seller/dashboard" class="back-link">Cancel and Go Back</a>
        </div>
    </div>

</body>
</html>