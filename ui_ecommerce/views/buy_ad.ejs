<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promote Your Items - UI Market</title>
    <style>
        /* 1. Base Styles (Consistent with Checkout) */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* 2. Container */
        .ad-container {
            background: white;
            width: 100%;
            max-width: 550px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background-color: #2c3e50; /* Matches Seller Dashboard Sidebar */
            padding: 30px;
            color: white;
            text-align: center;
        }

        .header h1 { margin: 0; font-size: 1.5rem; font-weight: 600; }
        .header p { margin: 10px 0 0; opacity: 0.8; font-size: 0.95rem; }

        .body { padding: 30px; }

        /* 3. Form Elements */
        label.section-label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #444;
            font-size: 0.95rem;
        }

        textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
            font-family: inherit;
            resize: vertical;
            margin-bottom: 25px;
            font-size: 1rem;
        }
        textarea:focus { outline: none; border-color: #007bff; }

        /* 4. Pricing Options (Radio Cards) */
        .pricing-options {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-bottom: 30px;
        }

        .price-option {
            display: flex;
            align-items: center;
            border: 2px solid #f0f2f5;
            padding: 15px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
            position: relative;
        }

        /* Highlight selected option */
        .price-option:hover { border-color: #bdc3c7; }
        
        /* Hide default radio */
        .price-option input[type="radio"] {
            margin-right: 15px;
            accent-color: #28a745;
            transform: scale(1.3);
        }

        .option-details { flex: 1; }
        .option-title { font-weight: bold; color: #333; display: block; }
        .option-price { color: #28a745; font-weight: bold; font-size: 1.1rem; }

        /* 5. Pay Button */
        .btn-pay {
            background-color: #28a745; /* Green for Payment */
            color: white;
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        .btn-pay:hover { background-color: #218838; }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #666;
            text-decoration: none;
            font-size: 0.9rem;
        }
        .back-link:hover { text-decoration: underline; }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="header">
            <h1>Boost Your Sales 🚀</h1>
            <p>Get your items pinned to the campus Homepage.</p>
        </div>

        <div class="body">
            <form action="/ads/paystack/initialize" method="POST">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                
                <label class="section-label">Ad Message</label>
                <textarea name="message" rows="3" placeholder="e.g. 50% OFF on all Textbooks! Check out my profile." required maxlength="100"></textarea>

                <label class="section-label">Select Duration</label>
                <div class="pricing-options">
                    
                    <label class="price-option" onclick="selectRadio(this)">
                        <input type="radio" name="plan" value="1_day_500" checked>
                        <div class="option-details">
                            <span class="option-title">24 Hours Boost</span>
                            <small style="color: #666;">Great for flash sales</small>
                        </div>
                        <div class="option-price">₦500</div>
                    </label>

                    <label class="price-option" onclick="selectRadio(this)">
                        <input type="radio" name="plan" value="3_day_1200">
                        <div class="option-details">
                            <span class="option-title">3 Days Boost</span>
                            <small style="color: #666;">Best value for weekends</small>
                        </div>
                        <div class="option-price">₦1,200</div>
                    </label>

                    <label class="price-option" onclick="selectRadio(this)">
                        <input type="radio" name="plan" value="7_day_2500">
                        <div class="option-details">
                            <span class="option-title">7 Days Boost</span>
                            <small style="color: #666;">Maximum visibility</small>
                        </div>
                        <div class="option-price">₦2,500</div>
                    </label>

                </div>

                <button type="submit" class="btn-pay">
                    <span>Pay & Publish Ad</span>
                </button>
            </form>

            <a href="/seller/dashboard" class="back-link">Cancel and Go Back</a>
        </div>
    </div>

    <script>
        function selectRadio(element) {
            // Remove border color from all
            document.querySelectorAll('.price-option').forEach(el => {
                el.style.borderColor = '#f0f2f5';
                el.style.backgroundColor = 'transparent';
            });
            // Add border color to selected
            element.style.borderColor = '#007bff';
            element.style.backgroundColor = '#f8f9fa';
        }
    </script>

</body>
</html>