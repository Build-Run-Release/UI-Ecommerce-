<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback | Campus Market</title>
    <link rel="stylesheet" href="/css/campus-theme.css">
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar-rewrite flex justify-between items-center">
        <a href="/" class="font-bold text-primary" style="font-size: 1.25rem;">CampusMarket</a>
        <a href="/" class="btn btn-sm btn-outline">Back to Home</a>
    </nav>

    <div class="container" style="max-width: 600px; margin-top: 3rem;">

        <div class="card">
            <h1 style="margin-bottom: 0.5rem;">Submit Feedback</h1>
            <p class="text-muted" style="margin-bottom: 1.5rem;">Have a suggestion, complaint, or bug report? Let us
                know.</p>

            <% if (typeof msg !=='undefined' && msg) { %>
                <div
                    style="background: #dcfce7; color: #166534; padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem; font-weight: bold;">
                    <%= msg %>
                </div>
                <% } %>

                    <form action="/feedback" method="POST" class="flex flex-col gap-4">
                        <input type="hidden" name="_csrf"
                            value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

                        <div class="grid grid-cols-2" style="gap: 1rem;">
                            <div>
                                <label class="text-xs font-bold text-muted uppercase">Name</label>
                                <input type="text" name="name" value="<%= user ? user.username : '' %>"
                                    class="input-base" required>
                            </div>
                            <div>
                                <label class="text-xs font-bold text-muted uppercase">Email</label>
                                <input type="email" name="email" value="<%= user ? user.email : '' %>"
                                    class="input-base" required>
                            </div>
                        </div>

                        <div>
                            <label class="text-xs font-bold text-muted uppercase">Topic</label>
                            <select name="message_type" class="input-base" required>
                                <option value="" disabled selected>Select a topic...</option>
                                <option value="complaint">Complaint / Report User</option>
                                <option value="suggestion">Suggestion / Feature Request</option>
                                <option value="bug">Report a Bug</option>
                                <option value="other">Other</option>
                            </select>
                        </div>

                        <div>
                            <label class="text-xs font-bold text-muted uppercase">Message</label>
                            <textarea name="message" class="input-base" rows="5"
                                placeholder="Describe your issue or idea..." required></textarea>
                        </div>

                        <button class="btn btn-primary" style="width: 100%;">Submit Feedback</button>
                    </form>
        </div>

    </div>

</body>

</html>