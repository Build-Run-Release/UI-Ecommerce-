<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UI Ecommerce - Home</title>
    <style>
        /* 1. Base Styles & Reset */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        /* 2. Navigation Bar */
        nav {
            background-color: #343a40; /* Dark Grey */
            color: white;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .nav-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ffffff;
            text-decoration: none;
        }

        .nav-links a {
            color: #ccc;
            text-decoration: none;
            margin-left: 20px;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: white;
        }

        .btn-logout {
            color: #ff6b6b !important; /* Red for logout */
        }

        /* 3. Hero Section (Welcome Banner) */
        .hero {
            background-color: white;
            padding: 40px 20px;
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 1px solid #ddd;
        }

        .hero h1 { margin: 0 0 10px; color: #2c3e50; }
        .hero p { color: #666; font-size: 1.1rem; }

        /* 4. Container for Content */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* 5. Ads Section (Styled as a yellow notice) */
        .ads-section {
            background-color: #fff3cd;
            border: 1px solid #ffeeba;
            color: #856404;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 30px;
        }
        .ads-section h3 { margin-top: 0; }

        /* 6. Product Grid Layout */
        .product-grid {
            display: grid;
            /* This creates columns that automatically fit the screen width */
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
            padding-bottom: 50px;
        }

        /* 7. Product Card Design */
        .product-card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
            display: flex;
            flex-direction: column;
        }

        .product-card:hover {
            transform: translateY(-5px); /* Lift up slightly on hover */
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .product-image {
            width: 100%;
            height: 200px;
            background-color: #e9ecef;
            object-fit: cover; /* Ensures image covers area without stretching */
        }

        .product-info {
            padding: 15px;
            flex-grow: 1; /* Pushes button to bottom */
            display: flex;
            flex-direction: column;
        }

        .product-title {
            font-size: 1.1rem;
            margin: 0 0 10px;
            color: #2c3e50;
        }

        .product-price {
            font-size: 1.25rem;
            font-weight: bold;
            color: #28a745; /* Green price */
            margin-bottom: 15px;
        }

        .btn-buy {
            margin-top: auto;
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            text-align: center;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: 0.2s;
        }

        .btn-buy:hover {
            background-color: #0056b3;
        }
    </style>
    <style>
        /* Profile Container */
        .profile-menu {
            position: relative;
            display: inline-block;
            cursor: pointer;
            margin-left: 20px;
        }

        /* The Circular Profile Image */
        .profile-img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #ddd;
            object-fit: cover;
            border: 2px solid white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            transition: transform 0.2s;
        }

        .profile-img:hover {
            transform: scale(1.05);
        }

        /* The Dropdown Box (Hidden by default) */
        .dropdown-content {
            display: none; /* Hidden */
            position: absolute;
            right: 0;
            top: 50px;
            background-color: white;
            min-width: 200px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
            border-radius: 8px;
            z-index: 1000;
            overflow: hidden;
            border: 1px solid #eee;
        }

        /* Dropdown Links */
        .dropdown-content a {
            color: #333;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            font-size: 0.95rem;
            transition: 0.2s;
            display: flex; /* Align icon and text */
            align-items: center;
            gap: 10px;
        }

        .dropdown-content a:hover {
            background-color: #f1f1f1;
            color: #007bff;
        }

        /* Separator Line */
        .dropdown-divider {
            height: 1px;
            background-color: #eee;
            margin: 0;
        }

        /* Show class for JS */
        .show { display: block; }
    </style>

    <script>
        function toggleProfileMenu() {
            document.getElementById("myDropdown").classList.toggle("show");
        }

        // Close the dropdown if the user clicks outside of it
        window.onclick = function(event) {
            if (!event.target.matches('.profile-img')) {
                var dropdowns = document.getElementsByClassName("dropdown-content");
                for (var i = 0; i < dropdowns.length; i++) {
                    var openDropdown = dropdowns[i];
                    if (openDropdown.classList.contains('show')) {
                        openDropdown.classList.remove('show');
                    }
                }
            }
        }
    </script>
</head>
<body>

    <nav>
        <a href="/" class="nav-brand">UI Ecommerce</a>
        <div class="nav-links">
            <% if (typeof user !== 'undefined' && user) { %>
        
            <span style="color: #ccc; margin-right: 10px;">Hi, <%= user.username %></span>

            <div class="profile-menu">
                <img src="https://ui-avatars.com/api/?name=<%= user.username %>&background=0D8ABC&color=fff" class="profile-img" onclick="toggleProfileMenu()">
            
                <div id="myDropdown" class="dropdown-content">
                
                    <% if (user.role === 'seller') { %>
                    <a href="/seller/dashboard">
                        <span>📊</span> Seller Dashboard
                    </a>
                    <% } else if (user.role === 'buyer') { %>
                    <a href="/buyer/dashboard">
                        <span>🛍</span> Buyer Dashboard
                    </a>
                    <% } else if (user.role === 'admin') { %>
                    <a href="/admin_dashboard">
                        <span>🛡</span> Admin Panel
                    </a>
                    <% } %>

                    <a href="/settings">
                        <span>⚙</span> Settings
                    </a>
                
                    <div class="dropdown-divider"></div>
                
                    <a href="/logout" style="color: #dc3545;">
                        <span>wq</span> Logout
                    </a>
                </div>
            </div>
        </div>

    <% } else { %>
        <a href="/login">Login</a>
        <a href="/signup" style="color: #28a745; font-weight: bold;">Sign Up</a>
    <% } %>
</div>
    </nav>

    <div class="hero">
        <h1>Welcome to the University of Ibadan Market</h1>
        <p>Buy and sell items conveniently on campus.</p>
    </div>

    <div class="hero">
        <h1>Welcome to the University of Ibadan Market</h1>
        <p>Buy and sell items conveniently on campus.</p>

        <div style="margin-top: 20px; max-width: 600px; margin-left: auto; margin-right: auto;">
            <form action="/" method="GET" style="display: flex; gap: 10px;">
                <input type="text" name="search" placeholder="What are you looking for? (e.g. iPhone, CHM 101)" 
                    style="flex: 1; padding: 12px; border: 1px solid #ccc; border-radius: 5px; font-size: 1rem;">
                
                <button type="submit" style="background-color: #007bff; color: white; border: none; padding: 12px 25px; border-radius: 5px; font-weight: bold; cursor: pointer;">
                    Search
                </button>
                
                <% if (typeof searchTerm !== 'undefined' && searchTerm) { %>
                    <a href="/" style="display: flex; align-items: center; color: #666; text-decoration: none; border: 1px solid #ddd; padding: 0 15px; border-radius: 5px; background: #fff;">Clear</a>
                <% } %>
            </form>
        </div>
    </div>

    <div class="container">
        
        <% if (typeof ads !== 'undefined' && ads && ads.length > 0) { %>
            <div class="ads-section">
                <h3>📢 Sponsored Messages</h3>
                <ul>
                    <% ads.forEach(function(ad) { %>
                        <li><%= ad.message %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>

        <h2 style="margin-bottom: 20px;">Latest Products</h2>
        
        <div class="product-grid">
            <% if (typeof products !== 'undefined' && products.length > 0) { %>
                <% products.forEach(function(product) { %>
                    <div class="product-card">
                        <img src="<%= product.image || 'https://via.placeholder.com/300x200?text=No+Image' %>" alt="Product Image" class="product-image">
                        
                        <div class="product-info">
                            <h3 class="product-title"><%= product.name || product.title %></h3>
                            <p style="color: #666; font-size: 0.9rem;"><%= product.description || 'No description available' %></p>
                            <div class="product-price">₦<%= product.price %></div>
                            <a href="/buy/<%= product.id %>" class="btn-buy">Buy Now</a>
                        </div>
                        <div class="card" style="margin-bottom: 30px;">
            <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #eee; padding-bottom: 15px; margin-bottom: 15px;">
                <h2 style="margin: 0;">My Wishcart</h2>
                <span style="background: #007bff; color: white; padding: 2px 8px; border-radius: 10px; font-size: 0.8rem;">
                    <%= cartItems.length %> Items
                </span>
            </div>

            <% if (cartItems && cartItems.length > 0) { %>
                <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px;">
                    <% cartItems.forEach(function(item) { %>
                        <div style="border: 1px solid #eee; border-radius: 8px; overflow: hidden; position: relative;">
                            
                            <form action="/cart/remove" method="POST" style="position: absolute; top: 10px; right: 10px; margin: 0;">
                                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                                <input type="hidden" name="cart_id" value="<%= item.cart_id %>">
                                <button type="submit" style="background: rgba(255,255,255,0.9); border: none; color: red; font-weight: bold; cursor: pointer; border-radius: 50%; width: 25px; height: 25px; display: flex; align-items: center; justify-content: center;">&times;</button>
                            </form>

                            <img src="<%= item.image_url || 'https://via.placeholder.com/300x200' %>" style="width: 100%; height: 150px; object-fit: cover;">
                            
                            <div style="padding: 15px;">
                                <h3 style="margin: 0 0 5px; font-size: 1rem;"><%= item.title || item.name %></h3>
                                <div style="color: #28a745; font-weight: bold; margin-bottom: 10px;">₦<%= item.price.toLocaleString() %></div>
                                
                                <a href="/buy/<%= item.id %>" style="display: block; text-align: center; background: #007bff; color: white; text-decoration: none; padding: 8px; border-radius: 5px;">Buy Now</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div style="text-align: center; color: #999; padding: 20px;">
                    Your wishcart is empty. <a href="/">Go shopping!</a>
                </div>
            <% } %>
        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No products available right now. Check back later!</p>
            <% } %>
        </div>

    </div>

</body>
</html>