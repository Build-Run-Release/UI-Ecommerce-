<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secure Checkout - UI Market</title>
    <link rel="stylesheet" href="/css/modern.css">
    <style>
        body {
            background-color: var(--bg-color);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .checkout-container {
            display: flex;
            background: white;
            width: 100%;
            max-width: 900px;
            border-radius: 16px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .product-section {
            flex: 1;
            padding: 40px;
            background: white;
            border-right: 1px solid var(--border-color);
        }

        .payment-section {
            flex: 0.8;
            padding: 40px;
            background-color: #fafbfc;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .product-preview {
            width: 100%;
            height: 220px;
            background-color: #f3f4f6;
            border-radius: 12px;
            margin-bottom: 20px;
            object-fit: cover;
        }

        .label {
            font-size: 0.85rem;
            color: var(--text-muted);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .value {
            font-size: 1.2rem;
            color: var(--text-main);
            font-weight: 500;
            margin-bottom: 20px;
        }

        .cost-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            color: var(--text-muted);
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 2px dashed var(--border-color);
            font-size: 1.4rem;
            font-weight: 800;
            color: var(--success-color);
        }

        @media (max-width: 768px) {
            .checkout-container {
                flex-direction: column;
                margin: 20px;
            }

            .product-section {
                border-right: none;
                border-bottom: 1px solid var(--border-color);
                padding: 30px;
            }

            .payment-section {
                padding: 30px;
            }
        }
    </style>
</head>

<body>

    <div class="checkout-container">

        <div class="product-section">
            <a href="/"
                style="text-decoration: none; color: var(--text-muted); font-size: 0.9rem; margin-bottom: 20px; display: inline-block;">&larr;
                Cancel and go back</a>
            <h2 style="margin-top: 0;">Order Summary</h2>

            <img src="<%= (product.image_url || product.image) && !(product.image_url || product.image).startsWith('http') ? '/uploads/' + (product.image_url || product.image) : (product.image_url || product.image || 'https://via.placeholder.com/400x200') %>"
                class="product-preview">

            <span class="label">Product</span>
            <div class="value">
                <%= product.name || product.title %>
            </div>

            <span class="label">Description</span>
            <div class="value" style="font-size: 1rem; color: #555;">
                <%= product.description || 'No description provided.' %>
            </div>

            <div
                style="margin-top: 30px; padding: 15px; background: #e0f2fe; border-radius: 8px; color: #0369a1; font-size: 0.9rem; border: 1px solid #bae6fd;">
                <strong>🛡 Safe & Secure:</strong> Funds are held in escrow until you receive your item.
            </div>
        </div>

        <div class="payment-section">
            <h2>Payment Details</h2>
            <p style="color: var(--text-muted); font-size: 0.95rem; margin-bottom: 30px;">
                Complete your purchase securely via Paystack.
            </p>

            <div class="cost-row">
                <span>Subtotal</span>
                <span>₦<%= parseInt(product.price).toLocaleString() %></span>
            </div>
            <div class="total-row">
                <span>Total</span>
                <span>₦<%= parseInt(total).toLocaleString() %></span>
            </div>

            <form action="/paystack/initialize" method="POST" style="margin-top: 30px;">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                <input type="hidden" name="productId" value="<%= product.id %>">
                <input type="hidden" name="amount" value="<%= total %>">
                <input type="hidden" name="serviceFee" value="<%= serviceFee %>">

                <button type="submit" class="btn btn-primary" style="width: 100%; padding: 15px; font-size: 1.1rem;">
                    Pay ₦<%= parseInt(total).toLocaleString() %>
                </button>
            </form>

            <div style="text-align: center; margin-top: 20px;">
                <img src="https://upload.wikimedia.org/wikipedia/commons/2/29/Paystack_Logo.png"
                    style="height: 20px; opacity: 0.6;">
            </div>
        </div>

    </div>

</body>

</html>