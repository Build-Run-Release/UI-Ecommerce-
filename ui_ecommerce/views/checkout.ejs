<!DOCTYPE html>
<html>
<head>
    <title>Checkout</title>
</head>
<body>
    <h1>Checkout</h1>
    <h3>Product: <%= product.title %></h3>
    <p><%= product.description %></p>
    <p>Price: $<%= product.price %></p>
    <p><em>(Includes 10% Service Fee of $<%= serviceFee.toFixed(2) %> which is handled internally)</em></p>
    
    <div style="border: 1px solid #ccc; padding: 20px; text-align: center;">
        <img src="https://upload.wikimedia.org/wikipedia/commons/1/1f/Paystack.png" alt="Paystack" width="150" style="margin-bottom: 20px;">
        <p>Secure Payment via Paystack</p>
        
        <form action="/paystack/initialize" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="hidden" name="productId" value="<%= product.id %>">
            <button type="submit" style="background-color: #3bb75e; color: white; padding: 10px 20px; font-size: 16px; border: none; border-radius: 4px; cursor: pointer;">
                Pay $<%= product.price %> with Paystack
            </button>
        </form>
    </div>
</body>
</html>
