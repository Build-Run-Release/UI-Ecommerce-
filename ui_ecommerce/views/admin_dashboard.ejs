<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        /* --- 1. Base Reset & Layout --- */
        * { box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            display: flex;
            min-height: 100vh;
            overflow-x: hidden; /* Prevent horizontal scroll when sidebar slides */
        }

        /* --- 2. Sidebar (The Moving Part) --- */
        .sidebar {
            width: 250px;
            background-color: #1a1a2e;
            color: white;
            display: flex;
            flex-direction: column;
            padding: 20px;
            
            /* Fixed position keeps it consistent */
            position: fixed;
            top: 0;
            left: 0;
            height: 100%;
            z-index: 100;
            
            /* ANIMATION MAGIC: Smoothly slide in/out */
            transition: transform 0.3s ease-in-out;
        }

        /* Sidebar Links Styling */
        .brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            text-decoration: none;
            margin-bottom: 30px;
            display: block;
            border-bottom: 1px solid #333;
            padding-bottom: 20px;
        }
        .nav-links a {
            display: block;
            padding: 12px;
            color: #a0a0c0;
            text-decoration: none;
            margin-bottom: 5px;
            border-radius: 6px;
            transition: 0.2s;
        }
        .nav-links a:hover, .nav-links a.active {
            background-color: #16213e;
            color: white;
        }
        .logout-btn { margin-top: auto; color: #ff6b6b !important; }

        /* --- 3. Main Content (Resizes based on screen) --- */
        .main-content {
            /* By default (Desktop), push content to the right of the fixed sidebar */
            margin-left: 250px; 
            flex: 1;
            padding: 30px;
            width: calc(100% - 250px);
            transition: margin-left 0.3s ease-in-out, width 0.3s ease-in-out;
        }

        /* --- 4. The Toggle Button --- */
        .toggle-btn {
            display: none; /* Hidden on Desktop */
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #1a1a2e;
            margin-right: 15px;
        }

        /* --- 5. Responsive / Mobile Logic (< 768px) --- */
        @media (max-width: 768px) {
            /* Make button visible */
            .toggle-btn { display: inline-block; }

            /* Hide Sidebar by moving it off-screen to the left */
            .sidebar {
                transform: translateX(-100%);
                box-shadow: none;
            }

            /* This class is added by JS to bring sidebar back */
            .sidebar.show-sidebar {
                transform: translateX(0);
                box-shadow: 5px 0 15px rgba(0,0,0,0.3);
            }

            /* Content takes full width on mobile */
            .main-content {
                margin-left: 0;
                width: 100%;
            }

            /* Overlay effect when menu is open */
            .overlay {
                display: none;
                position: fixed;
                top: 0; left: 0; right: 0; bottom: 0;
                background: rgba(0,0,0,0.5);
                z-index: 90;
            }
            .overlay.active { display: block; }
        }

        /* --- 6. Table & Card Styles --- */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .card { background: white; padding: 20px; border-radius: 12px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
        .card h3 { margin: 0 0 10px; color: #888; font-size: 0.9rem; }
        .card .value { font-size: 1.8rem; font-weight: bold; }
        
        .table-container {
            background: white; padding: 20px; border-radius: 12px;
            overflow-x: auto; /* Allows table scrolling on mobile */
        }
        table { width: 100%; border-collapse: collapse; min-width: 600px; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #eee; }
        
        /* Badges */
        .badge { padding: 4px 8px; border-radius: 4px; font-size: 0.8rem; color: white; }
        .role-admin { background: #000; } .role-seller { background: #007bff; } .role-buyer { background: #6c757d; }
    </style>
</head>
<body>

    <div class="overlay" id="overlay" onclick="toggleSidebar()"></div>

    <nav class="sidebar" id="sidebar">
        <div class="brand">
            ADMIN PANEL
            <span onclick="toggleSidebar()" style="float: right; cursor: pointer; display: block; font-size: 1.2rem; margin-top: -5px;" class="mobile-only-close">×</span>
        </div>
        <div class="nav-links">
            <a href="/admin_dashboard" class="active">Overview</a>
            <a href="#users">User Management</a>
            <a href="/">View Live Site</a>
            <a href="/logout" class="logout-btn">Logout</a>
        </div>
    </nav>

    <main class="main-content">
        <header style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 30px;">
            <div style="display: flex; align-items: center;">
                <button class="toggle-btn" onclick="toggleSidebar()">☰</button>
                <h1 style="margin: 0; font-size: 1.5rem;">Dashboard</h1>
            </div>
            <div>
                <span style="color: green; font-weight: bold; font-size: 0.9rem;">● Online</span>
            </div>
        </header>

        <div class="stats-grid">
            <div class="card" style="border-left: 5px solid #6610f2;">
                <h3>Total Users</h3>
                <div class="value"><%= users.length %></div>
            </div>
            <div class="card" style="border-left: 5px solid #28a745;">
                <h3>Revenue</h3>
                <div class="value">₦<%= (stats.total_revenue || 0).toLocaleString() %></div>
            </div>
            <div class="card" style="border-left: 5px solid #fd7e14;">
                <h3>Orders</h3>
                <div class="value"><%= stats.total_orders || 0 %></div>
            </div>
        </div>

        <div class="table-container" id="users">
            <h3>User Management</h3>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>User</th>
                        <th>Role</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(function(u) { %>
                    <tr>
                        <td>#<%= u.id %></td>
                        <td><%= u.username %></td>
                        <td><span class="badge role-<%= u.role %>"><%= u.role %></span></td>
                        <td>
                            <button style="padding: 5px 10px; cursor: pointer;">Manage</button>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </main>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('overlay');
            
            // This toggles the class that changes the CSS 'transform' property
            sidebar.classList.toggle('show-sidebar');
            overlay.classList.toggle('active');
        }

        // Optional: Hide Close "x" button on desktop via JS or just CSS
        const style = document.createElement('style');
        style.innerHTML = `
            @media (min-width: 769px) { .mobile-only-close { display: none !important; } }
        `;
        document.head.appendChild(style);
    </script>

</body>
</html>