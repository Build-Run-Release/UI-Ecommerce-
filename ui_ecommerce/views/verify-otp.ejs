<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Login | Campus Market</title>
    <link rel="stylesheet" href="/css/campus-theme.css">
</head>

<body style="display:flex; align-items:center; justify-content:center; height:100vh;">

    <div class="card" style="width: 100%; max-width: 400px; padding: 2.5rem; text-align: center;">

        <h2 class="text-primary font-bold" style="margin-bottom: 0.5rem;">Verify It's You</h2>
        <p class="text-muted" style="margin-bottom: 2rem;">We sent a 6-digit code to your email.</p>

        <% if (typeof error !=='undefined' && error) { %>
            <div
                style="background: #fee2e2; color: #b91c1c; padding: 0.75rem; border-radius: 8px; font-size: 0.9rem; margin-bottom: 1.5rem;">
                <%= error %>
            </div>
            <% } %>

                <form action="/verify-otp" method="POST" class="flex flex-col gap-4">
                    <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

                    <div>
                        <input type="text" name="otp" placeholder="0 0 0 0 0 0" required
                            class="input-base text-center font-bold" style="font-size: 1.5rem; letter-spacing: 0.5rem;"
                            maxlength="6" pattern="[0-9]*" inputmode="numeric">
                    </div>

                    <button class="btn btn-primary" style="width: 100%;">Verify Code</button>
                </form>

                <div style="margin-top: 1.5rem;">
                    <a href="/login" class="text-sm font-medium text-muted hover:text-primary">Back to Login</a>
                </div>
    </div>

</body>

</html>