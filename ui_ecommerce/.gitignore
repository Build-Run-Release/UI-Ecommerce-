node_modules/
.env
.DS_Store
dist/
coverage/
.vscode/
utils/firebase_service_account.json
# Note: We might want to track the placeholder json, but usually it's better to ignore it if it ever contained secrets. 
# However, if the code crashes without it, we need a template.
# The user currently has a placeholder file. I'll allow the json but ensure it's the placeholder.
# Actually, ignoring it entirely and providing a sample is safer.
# For now, I'll ignore the specific real file if the user renames it, but let's stick to standard ignores.
